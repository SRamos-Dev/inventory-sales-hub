diff --git a/README.md b/README.md
index 0000000..e69de29
--- a/README.md
+++ b/README.md
@@ -1,0 +1,9999 @@
+# 🛒 Inventory Sales Hub
+
+Sistema empresarial para la gestión integral de inventarios, ventas, usuarios, facturación y autenticación basado en **Spring Boot + Java + JWT**.
+
+Este README condensa y organiza el manual técnico completo del proyecto para que puedas copiar/pegar directamente en `README.md`. La documentación extensa y original está disponible como referencia en el manual del proyecto.
+
+---
+
+## 📚 Tabla de contenidos
+
+- [Resumen del proyecto](#resumen-del-proyecto)  
+- [Características principales](#características-principales)  
+- [Arquitectura](#arquitectura)  
+- [Estructura del repositorio](#estructura-del-repositorio)  
+- [Entidades principales (resumen)](#entidades-principales-resumen)  
+- [DTOs y contrato API](#dtos-y-contrato-api)  
+- [Seguridad y autenticación (JWT)](#seguridad-y-autenticación-jwt)  
+- [Instalación y ejecución](#instalación-y-ejecución)  
+- [Modo prueba (test)](#modo-prueba-test)  
+- [Endpoints principales (resumen)](#endpoints-principales-resumen)  
+- [Pruebas automatizadas](#pruebas-automatizadas)  
+- [Despliegue](#despliegue)  
+- [Buenas prácticas y tips](#buenas-prácticas-y-tips)  
+- [Contribución](#contribución)  
+- [Licencia](#licencia)  
+- [Referencia detallada](#referencia-detallada)
+
+---
+
+## Resumen del proyecto
+
+**Inventory Sales Hub** es una aplicación backend diseñada para simular y gestionar un entorno de comercio digital: productos, inventario, pedidos, facturas, pagos, usuarios y roles (ADMIN / USER). Está pensada para ser consumida por frontends (web o móvil) y para servir como base de un producto mínimo viable, laboratorio de pruebas o ejemplo educativo/empresarial.
+
+---
+
+## Características principales
+
+| Módulo | Funcionalidad |
+|---:|---|
+| Gestión de productos | CRUD, historial de cambios, búsqueda, filtros, paginación, ordenación y alertas de stock bajo. |
+| Categorías | CRUD y asociación a productos. |
+| Usuarios & Roles | Registro, login, gestión de roles, perfiles. |
+| Pedidos (Orders) | Creación, listado por usuario, detalle y gestión de estado. |
+| Detalles de pedidos | Líneas de pedido (cantidad, precio unitario). |
+| Facturación (Invoice) | Generación de facturas por pedido. |
+| Pagos (Pay) | Registro y consulta de estados de pago (simulados). |
+| Dashboard | Métricas y estadísticas administrativas. |
+| Seguridad | Spring Security + JWT; protección por roles. |
+| Modo prueba | DB separada + TestDataLoader para pre-cargar datos. |
+
+---
+
+## Arquitectura
+
+Patrón: **Spring MVC** (Modelo — Vista — Controlador).  
+Separación en capas: Model, Repository, Service, Controller, Security.
+
+### Componentes principales
+
+- **Model (entities)**: Clases `@Entity` que representan tablas.
+- **Repository**: Extiende `JpaRepository` para cada entidad.
+- **Service**: Lógica de negocio (AuthService, ProductService, etc.).
+- **Controller**: Endpoints REST (`@RestController`) que usan DTOs.
+- **Security**: `JwtProvider`, filtro JWT, `SecurityConfig`, manejo de `PasswordEncoder`.
+
+### Flujo típico de una petición (resumido)
+
+```
+Cliente -> Controller -> Service -> Repository -> Base de datos
+```
+
+(diagrama de alto nivel: ver `ARCHITECTURE.md` o manual completo).
+
+---
+
+## Estructura del repositorio
+
+```
+inventorysaleshub/
+ ├── src/
+ │   ├── main/
+ │   │   ├── java/com/yourpackage/
+ │   │   │   ├── controller/
+ │   │   │   ├── model/
+ │   │   │   ├── repository/
+ │   │   │   ├── service/
+ │   │   │   ├── security/
+ │   │   │   └── InventorysaleshubApplication.java
+ │   │   └── resources/
+ │   │       ├── application.properties
+ │   │       └── application-test.properties
+ ├── pom.xml
+ └── README.md
+```
+
+---
+
+## Entidades principales (resumen)
+
+> Cada entidad debe llevar `@Entity`, `@Id` y `@GeneratedValue`. Relaciones con `@ManyToOne`, `@OneToMany`, `@OneToOne`, `@ManyToMany` según corresponda. (Detalles de campos y relaciones en el manual).
+
+| Entidad | Descripción / relaciones claves |
+|---:|---|
+| `User` | name, email, password, role (ManyToOne Role), orders (OneToMany Order) |
+| `Role` | name (ADMIN / USER), users (OneToMany User) |
+| `Product` | name, description, price, stock, category, supplier, store, histories (OneToMany ProductHistory) |
+| `Category` | name, products (OneToMany Product) |
+| `Order` | dateCreated, status, total, user (ManyToOne User), orderDetails (OneToMany OrderDetails), invoice (OneToOne Invoice), pay (OneToOne Pay) |
+| `OrderDetails` | order (ManyToOne), product (ManyToOne), quantity, unitPrice |
+| `Invoice` | issueDate, totalAmount, order (OneToOne) |
+| `Pay` | method, status, order (OneToOne) |
+| `Supplier` | companyName, contact, products (OneToMany Product) |
+| `Store` | name, location, products (OneToMany Product) |
+| `ProductHistory` | product (ManyToOne), action, timestamp |
+
+---
+
+## DTOs y contrato API
+
+Se recomienda usar DTOs para **Request** y **Response** y evitar exponer entidades directamente.
+
+### DTOs comunes (ejemplos)
+- `ApiResponseDTO<T>`: `{ boolean success, String message, T data }`
+- `UserDTO`, `UserRequestDTO`
+- `ProductDTO`, `ProductRequestDTO`, `ProductUpdateDTO`
+- `OrderRequestDTO`, `OrderItemRequestDTO`, `OrderResponseDTO`, `OrderItemDTO`
+- `InvoiceDTO`, `InvoiceRequestDTO`
+- `PayDTO`, `PayRequestDTO`
+- `ProductHistoryDTO`
+
+**Ventajas**: seguridad (no exponer password, etc.), control de lo que acepta la API, validaciones con `@Valid`.
+
+---
+
+## Seguridad y autenticación (JWT)
+
+- **Dependencias**: `spring-boot-starter-security`, `jjwt` (api/impl/jackson).
+- **JwtProvider**: genera y valida tokens; contiene secret y tiempos de expiración.
+- **AuthService**: register, login; usa `PasswordEncoder` (BCrypt).
+- **JwtAuthFilter**: extrae token `Authorization: Bearer <token>`, valida y coloca `Authentication` en contexto.
+- **SecurityConfig**:
+  - `csrf().disable()`
+  - `permitAll()` para `/auth/**`
+  - reglas: `/products/**` (ADMIN), `/orders/**` (USER, ADMIN), etc.
+  - habilitar `@EnableMethodSecurity` para usar `@PreAuthorize` en métodos.
+
+**Flujo**:
+1. `POST /auth/register` -> crea usuario (password encriptada) y asigna rol.  
+2. `POST /auth/login` -> valida credenciales y devuelve `LoginResponseDTO` con token.  
+3. Peticiones protegidas incluyen header `Authorization: Bearer <token>`.
+
+---
+
+## Instalación y ejecución
+
+### Requisitos
+- Java JDK 17+
+- Maven 3.8+
+- MySQL 8.x
+
+### Clonar repo
+```bash
+git clone https://github.com/SRamos-Dev/inventory-sales-hub.git
+cd inventory-sales-hub
+```
+
+### Crear base de datos (MySQL)
+```sql
+CREATE DATABASE inventory_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
+```
+
+### Configurar `src/main/resources/application.properties`
+Ejemplo mínimo:
+```properties
+spring.datasource.url=jdbc:mysql://localhost:3306/inventory_db
+spring.datasource.username=root
+spring.datasource.password=root
+spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
+
+spring.jpa.hibernate.ddl-auto=update
+spring.jpa.show-sql=true
+spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
+```
+
+### Ejecutar
+```bash
+./mvnw clean package
+./mvnw spring-boot:run
+```
+O ejecutar el `jar`:
+```bash
+java -jar target/inventorysaleshub-0.0.1-SNAPSHOT.jar
+```
+
+---
+
+## Modo prueba (Test Mode)
+
+Para entorno de pruebas aislado:
+
+1. Crear DB de pruebas:
+```sql
+CREATE DATABASE inventory_test_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
+```
+
+2. Añadir `application-test.properties` en `resources` con:
+```properties
+spring.datasource.url=jdbc:mysql://localhost:3306/inventory_test_db
+spring.datasource.username=root
+spring.datasource.password=root
+spring.jpa.hibernate.ddl-auto=create-drop
+spring.jpa.show-sql=true
+spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
+```
+
+3. Ejecutar:
+```bash
+SPRING_PROFILES_ACTIVE=test ./mvnw spring-boot:run
+```
+
+En modo test la clase `TestDataLoader` precarga roles, usuarios demo y productos. (Ver implementación en `service/config` o `loader`).
+
+---
+
+## Endpoints principales (resumen)
+
+> Los endpoints pueden variar según el paquete/implementación; aquí un resumen típico:
+
+| Recurso | Métodos | Rutas |
+|---|---:|---|
+| Auth | POST | `/auth/register`, `/auth/login` |
+| Users | GET, POST, PUT, DELETE | `/users`, `/users/{id}` |
+| Products | GET, POST, PUT, DELETE | `/products`, `/products/{id}` |
+| Categories | CRUD | `/categories` |
+| Orders | POST, GET | `/orders`, `/orders/{id}`, `/orders/user/{userId}` |
+| Invoices | GET, POST | `/invoices`, `/invoices/{id}` |
+| Payments | GET, POST | `/payments`, `/payments/{id}` |
+| Dashboard | GET | `/dashboard/metrics` |
+
+Para detalles de request/response y validaciones, usa los DTOs del proyecto o la documentación OpenAPI/Swagger si está habilitada.
+
+---
+
+## Pruebas automatizadas
+
+Recomendado:
+- **JUnit 5** para tests unitarios.
+- **Mockito** para mocks de servicios/repositorios.
+- **Spring Boot Test** (`@SpringBootTest`) para integración.
+- **Testcontainers** si quieres levantar contenedores de MySQL durante pruebas de integración.
+
+Ejemplo comando:
+```bash
+./mvnw test
+```
+
+---
+
+## Despliegue
+
+Opciones comunes:
+- Dockerize: crear `Dockerfile` y `docker-compose` para app + MySQL.
+- CI/CD: GitHub Actions para `mvn -B -DskipTests=false verify` + build + deploy.
+- Variables sensibles: usar secrets (DB credentials, JWT secret) en el entorno (no en `application.properties` en repo).
+
+Ejemplo `Dockerfile` (esqueleto):
+```dockerfile
+FROM eclipse-temurin:17-jdk-jammy
+VOLUME /tmp
+COPY target/*.jar app.jar
+ENTRYPOINT ["java","-jar","/app.jar"]
+```
+
+---
+
+## Buenas prácticas y tips
+
+- Usar DTOs para proteger entidades.
+- Validar entradas con `@Valid` y constraints (`@NotBlank`, `@Email`, `@Size`).
+- Manejo centralizado de excepciones (`@ControllerAdvice`) que devuelva `ApiResponseDTO`.
+- No almacenar secretos en el repo; usar variables de entorno.
+- Documentar la API con Springdoc OpenAPI / Swagger.
+- Mantener commits atómicos y PRs revisables.
+- Añadir tests unitarios para cada servicio crítico.
+
+---
+
+## Contribución
+
+1. Fork del repositorio.  
+2. Crear rama feature: `git checkout -b feature/nombre`.  
+3. Hacer commits claros y tests.  
+4. Abrir Pull Request con descripción y checklist.
+
+---
+
+## Licencia
+
+Proyecto con licencia **MIT** — uso comercial y educativo permitido.
+
+---
+
+## Referencia detallada
+
+Documento base y manual técnico completo utilizado para construir esta guía: **Manual y guía de proyecto (Gestor de ventas e inventario)** — Sergio Esteban Ramos.
+
+---
+
+*(Fin del nuevo README.md)*